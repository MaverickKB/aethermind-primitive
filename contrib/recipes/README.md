# Integration Recipes

These recipes are examples. They are not the core primitive and are not included in the published Python package.

The universal v0.1.0 path is the CLI: any agent that can run shell commands can write layers.
